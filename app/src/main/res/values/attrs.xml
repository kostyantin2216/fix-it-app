<?xml version="1.0" encoding="utf-8"?>
<resources>

    <declare-styleable name="ButtonStyleHolder">
        <attr name="android:background" />
        <attr name="android:textSize" />
        <attr name="android:textColor" />
    </declare-styleable>

    <declare-styleable name="OngoingOrderView">
        <attr name="maxTradesmenToShow" format="integer" />
    </declare-styleable>

    <attr name="cancelDrawable" format="reference" />
    <attr name="clearButtonMode">
        <enum name="never" value="0" />
        <enum name="always" value="1" />
        <enum name="whileEditing" value="2" />
        <enum name="unlessEditing" value="3" />
    </attr>

    <declare-styleable name="ExpandablePanel">
        <attr name="body" format="reference" />
    </declare-styleable>

    <declare-styleable name="ToggleTextView">
        <attr name="onText" format="string" />
        <attr name="offText" format="string" />
        <attr name="onTextColor" format="color" />
        <attr name="offTextColor" format="color" />
        <attr name="on" format="boolean" />
    </declare-styleable>

    <declare-styleable name="CancelableEditText">
        <attr name="cancelDrawable" />
        <attr name="clearButtonMode" />
    </declare-styleable>

    <declare-styleable name="CancelableAutoCompleteTextView">
        <attr name="cancelDrawable" />
        <attr name="clearButtonMode" />
    </declare-styleable>

    <declare-styleable name="FloatingTextButton">
        <attr name="buttonIcon" format="reference" />
        <attr name="buttonText" format="string|reference" />
        <attr name="buttonTextColor" format="reference|color" />
        <attr name="buttonColor" format="reference|color" />
    </declare-styleable>

</resources>